import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Final-Ing-Soft-Ionic-React',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
